let wordButton = document.getElementsByClassName("similarWord");

/*
for (let i = 0; i < activeInput.length; i++){
    if (activeInput[i].value === "Cat") {
      chrome.storage.sync.get({ keyCat: [] }, (result) => {
        wordButton[0].innerText = result.keyCat[0];
        wordButton[1].innerText = result.keyCat[1];
        wordButton[2].innerText = result.keyCat[2];
      });
    } else if (activeInput[i].value === "Helo") {
        chrome.storage.sync.get({ keyHelo: [] }, (result) => {
            wordButton[0].innerText = result.keyHelo[0];
            wordButton[1].innerText = result.keyHelo[1];
            wordButton[2].innerText = result.keyHelo[2];
        });
    } else if (activeInput[i].value === "heldp") {
        chrome.storage.sync.get({ keyHeldp: [] }, (result) => {
            wordButton[0].innerText = result.keyHeldp[0];
            wordButton[1].innerText = result.keyHeldp[1];
            wordButton[2].innerText = result.keyHeldp[2];
        });
    }
}
*/
